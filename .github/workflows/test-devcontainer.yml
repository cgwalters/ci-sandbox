name: Test DevContainer

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - 'devenv/**'
      - 'common/.devcontainer/**'
      - '.github/workflows/test-devcontainer.yml'
      - 'Justfile'

env:
  REGISTRY: ghcr.io

jobs:
  test-podman:
    name: Test (podman, ${{ matrix.os }})
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        os: [debian]
        # TODO: c10s has PAM/sudo issues with devcontainer CLI's --userns=keep-id
        # include:
        #   - os: c10s

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up runner
        uses: bootc-dev/actions/bootc-ubuntu-setup@main

      - name: Build devcontainer image
        run: just devenv-build-${{ matrix.os }}

      - name: Test devcontainer
        run: just devcontainer-test ${{ matrix.os }}

  test-docker:
    name: Test (docker)
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Test devcontainer with Docker
        uses: devcontainers/ci@v0.3
        with:
          configFile: common/.devcontainer/devcontainer.json
          push: never
          runCmd: bash devenv/devenv-selftest.sh
